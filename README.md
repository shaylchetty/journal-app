# Journal App

Day One–style journal app.

## Repo layout
- `services/api` — Go backend API
- `apps/web` — React frontend
- `infra` — local/dev infrastructure (docker-compose, etc.)
- `docs` — project documentation

## Dev (coming soon)
Will add a one-command local dev environment (Postgres + API + Web).
